DATABASE_URL=postgresql+asyncpg://postgres:your_password%40@db.xxx.supabase.co:5432/postgres
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
WEBHOOK_SECRET=change-me

# Option A — point to a JSON file (recommended for complex configs):
MCP_SERVERS_FILE=./mcp_servers.json

# Option B — inline JSON (simple/empty):
# MCP_SERVERS={}
